# Requirements for Working NFL Scraper

# Core dependencies
requests>=2.31.0
beautifulsoup4>=4.12.0
pandas>=2.0.0
openpyxl>=3.1.0

# Fast HTML parser (required for correct parsing)
lxml>=4.9.0

# Progress bars (optional but recommended)
tqdm>=4.65.0
